server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/won_invest_channel_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: won_user
    password: won_local_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

jwt:
  secret: won-invest-channel-jwt-secret-key-32chars!!

feign:
  common-server-url: http://localhost:8080
